# 오케스트레이션 (핵심)
# 멀티 에이전트 협업 프레임워크
# NOTE: 1.14.1 에서 검증된 ReAct 파서(Final Answer: 포맷)에 FakeProvider가 의존하므로
#       메이저/마이너 변경은 Phase 2 테스트 재검증과 함께 이뤄져야 함.
crewai==1.14.1
# CrewAI 기본 제공 도구 모음
crewai-tools==1.14.1
# 상태 기반 에이전트 그래프 오케스트레이션
langgraph>=0.2.0
# LLM 애플리케이션 체인 구성 프레임워크
langchain>=0.3.0
# LangChain용 Anthropic Claude 연동 어댑터
langchain-anthropic>=0.2.0

# LLM Provider 시스템
# Claude Code MAX 구독을 경유해 Claude를 호출하는 Anthropic 공식 SDK
claude-agent-sdk>=0.1.60
# 비동기 런타임 추상화(asyncio/trio 공통 API)
anyio>=4.0.0

# 모니터링
# AI 에이전트 호출 추적 및 모니터링
langfuse>=2.0.0

# 환경변수
# .env 파일에서 환경 변수 로드
python-dotenv>=1.0.0

# 데이터 처리
# 표 형식 데이터 분석 및 조작
pandas>=2.0.0
# Excel(.xlsx) 파일 읽기/쓰기
openpyxl>=3.1.0
# 데이터 모델 검증 및 스키마 정의
pydantic>=2.5.0

# 유틸리티
# YAML 설정 파일 파싱
pyyaml>=6.0.0
# 컬러풀한 콘솔 출력 및 진행률 표시
rich>=13.0.0

# 빌드 도구 (Phase 4.5 — PR #36)
# Python → 단일 실행파일(.exe) 패키저. build_executor 가 subprocess 로 호출.
# 6.20+ 부터 Python 3.13 안정 지원.
pyinstaller>=6.20.0

# QA 도구 (Phase 7 — PR #44 / PR #52)
# GUI 자동화 + 스크린샷 캡처. gui_test_executor 가 GUI 산출물 검증에 사용.
# 본 PR #52 에서 정식 의존성 화 — 미설치 시 graceful skip 되지만, GUI 풀체인의
# active QA gating 을 위해 필수.
pyautogui>=0.9.54

# 개발/테스트 (Phase 2 — pytest 하네스)
# 단위·통합 테스트 러너
pytest>=8.0.0
# monkeypatch 확장 및 MockerFixture 제공
pytest-mock>=3.12.0
# 네트워크 차단 플러그인.
# NOTE: Windows ProactorEventLoop의 내부 socketpair 호출까지 막아버려 autouse로는
#       사용 불가. Linux CI에서 `pytest --disable-socket` 플래그로 opt-in 적용 전제.
pytest-socket>=0.7.0
